<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.julius.spring.boot</groupId>
	<artifactId>eth-web3</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>eth-web3</name>
	<description>Demo project for Spring Boot &amp; web3j</description>

	<properties>
		<java.version>21</java.version>
		<web3j-maven-plugin.version>4.14.0</web3j-maven-plugin.version>
		<web3j-core.version>4.14.0</web3j-core.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.web3j</groupId>
			<artifactId>core</artifactId>
			<version>${web3j-core.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.8.17</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<dependencies>
					<dependency>
						<groupId>org.web3j</groupId>
						<artifactId>web3j-sokt</artifactId>
						<version>0.7.0</version>
					</dependency>
				</dependencies>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.5.0</version>
				<executions>
					<execution>
						<id>npm-install</id>
						<phase>initialize</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>npm</executable>
							<workingDirectory>${basedir}</workingDirectory>
							<arguments>
								<argument>install</argument>
							</arguments>
						</configuration>
					</execution>
					<execution>
						<id>deploy-contracts</id>
						<goals>
							<goal>java</goal>
						</goals>
						<configuration>
							<mainClass>com.julius.spring.boot.ethweb3.demo.PipelineLocalTestPrepDemo</mainClass>
							<includePluginDependencies>false</includePluginDependencies>
						</configuration>
					</execution>
				</executions>
			</plugin>


			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.3.1</version>
				<executions>
					<execution>
						<id>copy-contracts-to-target-contracts</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${basedir}/target/contracts</outputDirectory>
							<resources>
								<resource>
									<directory>${basedir}/node_modules/</directory>
									<includes>
										<include>@openzeppelin/contracts/token/ERC20/*.sol</include>
										<include>@openzeppelin/contracts/token/ERC20/extensions/IERC20Metadata.sol</include>
										<include>@openzeppelin/contracts/utils/Context.sol</include>
										<include>@openzeppelin/contracts/interfaces/draft-IERC6093.sol</include>
										<include>hardhat/console.sol</include>
									</includes>
								</resource>
								<resource>
									<directory>${basedir}/src/main/resources/contracts/</directory>
									<includes>
										<include>*.sol</include>
									</includes>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- added to create Solidity java wrapper classes -->
			<plugin>
				<groupId>org.web3j</groupId>
				<artifactId>web3j-maven-plugin</artifactId>
				<version>${web3j-maven-plugin.version}</version>
				<dependencies>
					<dependency>
						<groupId>org.web3j</groupId>
						<artifactId>web3j-sokt</artifactId>
						<version>0.7.0</version>
					</dependency>
				</dependencies>
				<executions>
					<execution>
						<id>generate-resources-web3j</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>generate-sources</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<packageName>com.julius.spring.boot.ethweb3</packageName>
					<sourceDestination>${basedir}/target/generated-sources/contracts</sourceDestination>
					<abiSourceFiles>
						<directory>${basedir}/src/main/resources/abi-disabled</directory>
						<includes>
							<include>**/*.abiignore</include>
						</includes>
					</abiSourceFiles>
					<soliditySourceFiles>
						<directory>${basedir}/target/contracts</directory>
						<includes>
							<include>*.sol</include>
							<include>@openzeppelin/**/*.sol</include>
							<include>hardhat/*.sol</include>
						</includes>
					</soliditySourceFiles>
					<pathPrefixes>
						<!--suppress UnresolvedMavenProperty -->
						<pathPrefix>@openzeppelin=./@openzeppelin</pathPrefix>
						<pathPrefix>hardhat=./hardhat</pathPrefix>
					</pathPrefixes>
				</configuration>
			</plugin>

			<!-- added to add the generated solidity java wrapper sources to /target/ folder -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>add-processed-solidity-wrapper</id>
						<phase>process-resources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${basedir}/target/generated-sources/contracts</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>

</project>
